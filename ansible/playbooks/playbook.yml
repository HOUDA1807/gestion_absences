---
- name: Install Apache on target servers
  hosts: localhost
  become: true
  become_user: root
  become_method: sudo

  tasks:
    - name: Update apt package index
      apt:
        update_cache: yes

    - name: Install Apache
      apt:
        name: apache2
        state: present
